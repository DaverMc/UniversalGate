+ MAn kann bei einem bett nur einen von beiden abbauen, ohne dass des komplett zerstört wird
+ Sand/Gravel etc. fällt herunter bei platzieren in der luft
+ Tag und Nacht Zyklus in den Welten ausschalten
+ Wetter in den welten ausschalten
+ Natürliches Entity Spawning disablen
+ Advancements ausschalten
+ Stoplag Toggle Command
+ Wenn Spieler von der Map leavt wird sie nicht geunloaded
+ Creative Items Debug Stick hinzufügen
+ Bone Meal deaktivieren
+ Portale deaktivieren
+ Golems deaktivieren mit Permission
+ TNT explodiert und macht blöcke kaputt
+ Farmland verändert sich durch wasser
+ Minecarts Bewegung deaktivieren
+ GameMode Creative
+ Armorstand kann nicht platziert werden
+ Reopen Task Command fehlt
+ Import und Export Ordner werden nicht neu erstellt
+ TabList sortieren
+ Armorstand Tool programmieren
+ Void-Welten können nicht erstellt/geladen werden
+ Statue Pose Save/List/Load füllen
+ TabList Header und Footer werden nicht aktualisiert wenn ein spieler neu joint oder leaved
+ Armorstand cannot be rotated
+ Item Benennen / Set Lore / Enchant(Glow)
+ Suggestions nur das was auch Permissions technisch erlaubt ist anzeigen
+ Archive und Activate Feature einbauen für ungenutzte Dimensionen
+ UUID für Dimensionen einführen vor allem im Bezug auf Tasks
+ Dimension Rename
+ Armorstand Clone Armorstand fliegt irgendwo hin
+ Languagesystem falls Sprache nicht vorhanden ist wirds nicht auf default umgeleitet
+ Export mit einem zusätzlichen Ordner als root (und dieser Ordner soll bennenbar sein)
+ Manchmal wird LOADED nicht zurück gesetzt.
+ Localize Key bei Stoplag

=== Bugs ===:
- Nachricht wenn teleport abgebrochen wird
- Dimension List Command zeigt alle nicht nur die sichtbaren an
- /w acceptInvite statt dem komischen
- /w allowed list statt /w allowed
- Eigenen /gamemode command womit man
/w invite ist nur view only
/build <player> fügt einen inviteten Spieler, wird entfernt sobald die welt wieder verlassen wird
- UserArgument erweitern, dass wenn ein nicht vorgeschlagen Name eingegeben wird der OfflinePlayer gefetcht wird
- /w chunkGen generiert die Chunks einer welt in einem radius
- Nether als Generators hinzufügen
- Man kann keine categorien zum erstellen von dimensionen verweden
- In der Dimension List sieht man nicht welche maps aktuell geladen sind
- Dimension List filter ist nun nicht mehr Case-Sensitive
- !Die default world macht komplett faxen mit allen commands
- SUFFIX entfernen -> Geändert
- eNot allowed nachricht Name der Welt entfernen
- acceptInvite lässt einen nicht in die -> Schein Fixed
-Seed als Argument fürs erstellen von Dimension einbauen
- allowed umbauen auf local zusätzlich umbauen
- Die Dimensionsliste soll ein Argument haben womit nach Anfängen gesucht werden kann. Als Vorschläge sollen die Categorien kommen, aber man kann auch freitext benutzen
- Der Spieler name muss mit einem TextDisplay verlinkt werden

Von Kategorie Prefix zu Kategorie Namen parsen für z.B. dimension info

LuckPerms PlayerFetcher gegen eigenen playerFetcher tauschen
End DimensionGen
help command einbauen für die Commands speed,gamemode,nightvision
Generieren eines Chunks soll laufen ohne dass ein Spieler auf der Map sein muss
Wenn das erstellen fehlschlägt, Dateien, oder Datenbankeinträge löschen
Archivierte Welten sollen als .tar.gz in einem Ordner gespeichert werden sodass der WorldContainer cleaner ist
(Message Channels)
(Portalblöcke & Feuerblöcke in Custom Items)
(DataPack Creator für zum Beispiel DeathRun Hindernisse oder Custom Biome)
